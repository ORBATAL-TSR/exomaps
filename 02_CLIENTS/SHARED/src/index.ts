/**
 * @exomaps/shared — Cross-platform shared code for ExoMaps clients.
 *
 * Provides types, API client, shader sources, constants, and utilities
 * consumed by Web, Desktop (Tauri), and Mobile (React Native) clients.
 */

export * from './types';
export * from './api';
export * from './constants';
export * from './utils';
// Shaders are imported separately via '@exomaps/shared/shaders'
